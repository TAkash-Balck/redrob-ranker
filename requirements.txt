# Core
tqdm==4.66.4
pyyaml==6.0.1
numpy==1.26.4

# DataFrame / CSV
pandas==2.2.2

# Streamlit UI
streamlit==1.35.0
plotly==5.22.0

# Date parsing
python-dateutil==2.9.0.post0

# Testing
pytest==8.2.2
pytest-cov==5.0.0

# Optional speedup (pure Python fallback if unavailable)
# orjson is significantly faster than stdlib json for large JSONL files
orjson==3.10.3
